{
  "name": "masmax-streaming-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Catalogo web demo de streaming con React, NestJS, PostgreSQL y Prisma.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace apps/backend\" \"npm run dev --workspace apps/frontend\"",
    "build": "npm run build --workspace apps/backend && npm run build --workspace apps/frontend",
    "lint": "npm run lint --workspace apps/backend && npm run lint --workspace apps/frontend",
    "typecheck": "npm run typecheck --workspace apps/backend && npm run lint --workspace apps/frontend",
    "test": "npm run test --workspace apps/backend",
    "prisma:generate": "npm run prisma:generate --workspace apps/backend",
    "prisma:migrate": "npm run prisma:migrate --workspace apps/backend",
    "seed": "npm run prisma:seed --workspace apps/backend",
    "seed:admin": "npm run prisma:seed:admin --workspace apps/backend",
    "seed:demo": "npm run prisma:seed:demo --workspace apps/backend"
  },
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
