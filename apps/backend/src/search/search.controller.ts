import { Controller, Get, Query, UseGuards } from '@nestjs/common'; import { Type } from 'class-transformer'; import { IsInt, IsOptional, IsString, Max, Min, MinLength } from 'class-validator'; import { JwtAuthGuard } from '../common/jwt-auth.guard'; import { PrismaService } from '../prisma/prisma.service';
class SearchDto { @IsString() @MinLength(2) q!:string; @IsOptional() @Type(()=>Number) @IsInt() @Min(1) page=1; @IsOptional() @Type(()=>Number) @IsInt() @Min(1) @Max(50) limit=20; @IsOptional() @IsString() type?:string; @IsOptional() @IsString() genre?:string; @IsOptional() @Type(()=>Number) @IsInt() year?:number; }
@UseGuards(JwtAuthGuard) @Controller('search') export class SearchController { constructor(private readonly prisma:PrismaService){} @Get() async search(@Query() dto:SearchDto){const skip=(dto.page-1)*dto.limit;const text={contains:dto.q,mode:'insensitive' as const};const [series,movies,episodes]=await Promise.all([dto.type&&dto.type!=='series'?[]:this.prisma.series.findMany({where:{deletedAt:null,title:text,year:dto.year,genres:dto.genre?{some:{slug:dto.genre}}:undefined},include:{genres:true},skip,take:dto.limit}),dto.type&&dto.type!=='movies'?[]:this.prisma.movie.findMany({where:{deletedAt:null,status:'PUBLISHED',title:text,releaseYear:dto.year,genres:dto.genre?{some:{slug:dto.genre}}:undefined},include:{genres:true},skip,take:dto.limit}),dto.type&&dto.type!=='episodes'?[]:this.prisma.episode.findMany({where:{deletedAt:null,title:text},include:{series:true},skip,take:dto.limit})]);return{query:dto.q,page:dto.page,limit:dto.limit,series,movies,episodes}} }
